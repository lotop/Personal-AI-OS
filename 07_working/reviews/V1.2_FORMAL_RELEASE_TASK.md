# Personal AI OS V1.2.0 Formal Release Task

> 状态：`DONE`
>
> 日期：`2026-09-02`
>
> Task ID：`paos-20-v1-2-formal-release`
>
> Owner：`paos-20-v1-2-formal-release`

## Objective

完成 Personal AI OS V1.2.0 的正式发布合流，通过冷克隆与离线 Git Bundle 双路径恢复演练，生成不可变 V1.2 恢复证据，打上本地 annotated tag `v1.2.0` 并通过 M1~M6 全部门禁验收。

## Scope

- 合并 `codex/v1.2-canonical-promotion` 到本地 `main`。
- 将 `SYSTEM.toml`、`AGENTS.md`、`projects.toml` 与 `CHANGELOG.md` 对齐到 `1.2.0 / PAOS-REL-006`。
- 重新生成并验证适配器，部署至项目根路径。
- 生成离线 Git Bundle（`06_deployment/recovery_artifacts/v1.2.0.bundle`），计算 SHA-256。
- 运行双路径恢复演练并记录 `recovery_evidence.toml` 与 `RECOVERY_DRILL.md`。
- 记录 `DECISIONS.md`（`PAOS-REL-006`）。
- 创建本地 annotated tag `v1.2.0`。
- 运行 `release_audit.py --require-release-ready` 与 `ci_gate.py` 完成验收。

## Excluded

- 不 Push 分支或 Tag；远端同步由 Founder 自行在 VS Code 独立操作。
- 不处理外部真实业务项目数据传输或未授权的 Live Runtime。

## Validation

- Implementation Freeze Commit：`65fda3fbfbb0d250694799ff7662ae9286dc5989`。
- Main Merged：Fast-forward PASS。
- Bundle SHA-256：`5a51f344438e458b6dfa7e60782ba8dc8fb9194d5535df0c1c5bcd2f67bceb46`。
- Tree Digest V0.2 SHA-256：`59c157bb0686f492640ca094e762bec6e45b3ad9a37a876da429ad8f7052a1fd`。
- Cold Clone / Bundle Clone CI 8/8 PASS、`ADAPTERS_OK`、`git fsck --full` PASS。
- 单元与集成测试：85 项全通过。
