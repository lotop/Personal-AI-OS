# Personal AI OS V1.2.1 Platform Migration Task

> 状态：`DONE`
>
> 日期：`2026-09-02`
>
> Task ID：`paos-21-antigravity-platform-migration`
>
> Owner：`paos-21-antigravity-platform-migration`

## Objective

全面将 Personal AI OS 中的历史 `gemini-cli` 平台标识与适配层迁移更正为 `antigravity-cli`（Google DeepMind Antigravity CLI / AGY），消除运行时未安装失真状态，刷新真实 Runtime Evidence，完成双路径恢复演练并打上本地 annotated tag `v1.2.1`。

## Scope

- 平台配置：`00_system/compatibility/adapter_profiles.toml`、`platforms.toml`、`capabilities.toml` 升级为 `antigravity-cli`。
- 适配器生成与部署：重构 `05_harness/generate_adapters.py`，生成 `03_adapters/antigravity-cli/`，部署至项目根 `.gemini/settings.json`。
- 部署规范：重写为 `06_deployment/ANTIGRAVITY_DEPLOYMENT.md`。
- 运行时账本：在 `02_registry/runtimes.toml` 中将 `antigravity-cli` 状态更正为 `INSTALLED`（实测 PASS）。
- Schemas：更新 Task、Hook、Runtime、System Schemas 平台枚举。
- Harness 门禁：更新 `release_audit.py` M4 适配器平台依赖与测试套件。
- 技能与文档：更新 `create-paos-project` 技能、`SKILLS_ARCHITECTURE.md` 与 `README.md`。
- 版本升级：升级为 `1.2.1 / PAOS-REL-007`，执行双路径恢复演练，打上本地 tag `v1.2.1`。

## Excluded

- 不 Push 分支或 Tag；远端同步由 Founder 自行在 VS Code 独立操作。
- 不修改 Claude Code 与 Codex 核心业务逻辑。

## Validation

- Implementation Freeze Commit：`c8e3ecc04a4dbc5a5d4f6995f10e97a9dd6aa42a`（原为 `69b75a9601156c374c42b75cd594a279be7ae6b2`，因发布后的 harness 修复而更正）。
- Tested Release Commit：`c8e3ecc04a4dbc5a5d4f6995f10e97a9dd6aa42a`。
- Bundle SHA-256：`4fbcc965afabf2727cc199a0a674601621713ce1c3916723942616b9e15bcf98`。
- Tree Digest V0.2 SHA-256：`fc974fecf69c5ba29b16f71ca25e34427282631740b8fbc6156d92b66659a848`。
- Cold Clone / Bundle Clone 双路径演练：CI 8/8 PASS、`ADAPTERS_OK`、`git fsck --full` PASS。
- 单元与集成测试：86 项全通过。

### 发布后更正记录

本卡片的 Validation 曾于 `2026-09-02` 被原地改写，覆盖了原 V1.2.1 的证据组
（`ebc1c79` / `e5e2d6e9…` / `bfdc451b…`）；annotated tag `v1.2.1` 同时由 `299f25a` 重新指向 `15cfee2`。
该重打与覆盖的完整记录见 `PAOS-023` 与 `07_working/reviews/V1.2.1_POST_RELEASE_REMEDIATION_TASK.md`。
