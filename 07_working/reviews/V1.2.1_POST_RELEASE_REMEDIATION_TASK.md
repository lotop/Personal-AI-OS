# V1.2.1 Post-Release Remediation Task Card

> 资产类别：`SOURCE`
>
> 状态：`WORKING`
>
> 日期：`2026-09-02`
>
> Task ID：`paos-22-v1-2-1-post-release-remediation`
>
> Owner：Founder / `paos-22-v1-2-1-post-release-remediation`
>
> Approval Reference：`PAOS-023`

## Objective

修复 V1.2.1 发布之后遗留与新引入的证据完整性、门禁盲区与 Dashboard 安全缺陷，使账本与实际状态一致，并让"重复演练覆盖物证"与"BLOCKED 被显示成 PASS"两类问题在机制上不可再发生。

## Scope

- `SYSTEM.toml` 与 `02_registry/runtimes.toml` 的状态漂移更正。
- Recovery Artifact 命名规范与 M5 强制断言。
- `05_harness/ci_gate.py` 的 Release Audit 漏报修复。
- `dashboard/` 的网络暴露、静态根、CORS 与伪造门禁数据修复。
- `DECISIONS.md` 悬空引用更正与 `PAOS-023` 记录。

## Out of Scope

- 不重打、不移动、不删除任何 annotated tag。
- 不执行 `git push`；远端同步由 Founder 自行操作。
- 不发起 V1.2.2 Release，不修改 `approved_baseline.version` / `git_tag`。
- 不执行破坏性 GC，不清理 `99_temp/` 既有内容。

## Read Set

`SYSTEM.toml`、`02_registry/**`、`05_harness/**`、`06_deployment/recovery_artifacts/**`、`07_working/reviews/**`、`dashboard/**`、`DECISIONS.md`、`CHANGELOG.md`

## Write Set

- `SYSTEM.toml`
- `02_registry/runtimes.toml`、`02_registry/tasks.toml`
- `05_harness/ci_gate.py`、`05_harness/release_audit.py`、`05_harness/test_release_audit.py`
- `06_deployment/recovery_artifacts/v1.2.1-c8e3ecc0.bundle`（重命名，内容不变）
- `07_working/reviews/recovery_evidence.toml`、`RECOVERY_DRILL.md`、`V1.2.1_PLATFORM_MIGRATION_TASK.md`、本文件
- `dashboard/server.py`、`dashboard/index.html`、`dashboard/README.md`
- `DECISIONS.md`、`CHANGELOG.md`

## Dependencies

- `PAOS-REL-007`（V1.2.1 Release 批准）
- `paos-21-antigravity-platform-migration`

## 直接修改 main 的例外理由

Founder 于本轮明确授权修复。变更属于事实纠错（状态漂移、悬空引用）、已知缺陷修复（门禁漏报、Dashboard 网络暴露）与不改变对外契约的证据命名规范，范围小、可通过 `git revert` 回滚，且不触及 Tag、Push、Release Approval 或部署授权。按 `AGENTS.md` 例外条款记录于本 Task Card。

## 记录：v1.2.1 Tag 重新指向

`2026-09-02` 的 Antigravity 会话在 V1.2.1 发布后修复了 harness 的 git 子进程环境隔离问题（`c8e3ecc0`），随后将 annotated tag `v1.2.1` 由 `299f25a` 重新指向 `15cfee2`，并原地覆盖了恢复证据与 Bundle 物证。该重打没有留下 Decision 或 Task 记录。

本任务不撤销该重打（撤销会使已推送的 main 与本地 tag 再次分叉），而是补齐记录：

- 原 v1.2.1 Release Commit：`299f25a6d863b5d4c70540af33fac96ff7bb7e31`（现已无 ref 指向，仅存在于历史）
- 现 v1.2.1 Release Commit：`15cfee26879d9aeecf878428e8859b64cdde036d`
- 更正后的 Implementation Freeze Commit：`c8e3ecc04a4dbc5a5d4f6995f10e97a9dd6aa42a`
- 期间提交 `03a7673` 曾写入不存在的 commit hash `c8e3ecc5b9a8cfd6f83ec555a6d59bdf11467438`，由 `15cfee2` 更正为真实值 `c8e3ecc04a4d...`；M5 的 tree digest 断言会拒绝该假值，但该提交仍先于门禁完成并进入历史。

## Expected Output

- 账本与实际状态一致，无已知状态漂移。
- `ci_gate.py` 默认 profile 暴露 Release Audit 的真实 `overall`。
- M5 拒绝未内嵌 Tested Commit 前缀的 Bundle 文件名。
- Dashboard 只监听回环、静态根收紧、无 CORS 通配、门禁数据来自真实审计。
- `DECISIONS.md` 记录 `PAOS-023`。

## Acceptance Criteria

- `validate_repository.py`：`ERRORS=0`。
- `ci_gate.py --profile local-offline`：无 `FAIL`，且 `release-state` 项如实显示 `overall`。
- 全部单元与集成测试通过。
- Dashboard 冒烟：`/.git/config` 与 `/SYSTEM.toml` 返回 404，`/api/overview` 无 `Access-Control-Allow-Origin`，`gates` 与 `release_audit.py` 一致。
- 未产生 Tag、Push 或 Release Approval。

## Validation

- `validate_repository.py`：`ERRORS=0 WARNINGS=0`（工作树提交后）。
- `ci_gate.py`：8 项检查无 FAIL，新增 `release-state` 项如实显示 `overall`。
- 单元与集成测试：87 项全通过（release_audit 20、schema 15、temp_cleanup 13、tree_digest 4、factory 20、deployment 15）。
- Factory 端到端演练：Dry Run 与 `--apply --git` 在临时目录完成，12 个文件与 Git 仓库全部落地，`.paos-init.json` 保持 `PROVISIONAL`。
- Dashboard 冒烟：`/` 200、`/api/overview` 200 且无 CORS 头、`/guide` 404、`/.git/config` 404、`/SYSTEM.toml` 404。
- 未执行 Tag、Push、部署或 GC。

## 遗留未处理项

- `99_temp/` 累积 11 个未 apply 的 GC Plan 与约 276KB Quarantine；`destructive_gc_enabled = false`，清理需 Founder 单独授权。
- `02_registry/runtimes.toml` 中 `antigravity-cli` 的 `version = "CURRENT"` 仍非可复核版本证据，已在 `issues` 中记录待办。
- 本轮变更尚未发布；`approved_baseline` 仍为 `v1.2.1 / 15cfee2`，M5/M6 会在提交后如实转为 `STALE`/`BLOCKED`，等待 Founder 决定是否发起 V1.2.2。
