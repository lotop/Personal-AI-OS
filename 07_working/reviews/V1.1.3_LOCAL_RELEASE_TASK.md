# Personal AI OS V1.1.3 Local Release Task

> 状态：`DONE`
>
> 日期：`2026-09-01`
>
> Task ID：`paos-17-v1-1-3-local-release`
>
> Owner：`paos-17-v1-1-3-local-release`

## Objective

修复 PAOS-016 对 Approved Template Manifest 的原地改动，完善 Release Evidence 的可验证边界，并将已完成的 Post-V1.1.2 整改正式发布为本地 `v1.1.3`。

## Scope

- 恢复 `project-base-pack` 与 `core-template-pack` Approved Manifest 到批准时内容。
- 将 Pack Kind 迁移到 Project Factory 自有配置，保持 Factory/M3 的安全分类能力。
- 移除 `approved_baseline.release_commit` 的 Git Commit 自引用设计；Release Commit 由 annotated tag 直接绑定。
- 为 M5 增加可定位、可哈希、可执行 `git bundle verify` 的本地 Bundle Artifact。
- 更新 V1.1.3 System、Project Registry、Router、Decision、Changelog 与 Release Evidence。
- 执行冷克隆和离线 Bundle 双路径恢复演练。
- 创建本地 annotated tag `v1.1.3`。

## Excluded

- 不 Push 分支或 Tag；远端同步由 Founder 在 VS Code 独立执行。
- 不处理、运行或验收 `dashboard/**`。
- 不执行真实业务项目 E2E、Claude/Gemini Live Runtime 或外部数据传输。
- 不修改已批准模板正文，不创建新 Template Approval。

## Read Set

- V1.1.2 Approved Baseline、PAOS-016 两个本地 Commit、Release Gate、Recovery 与 Template Governance 文件。

## Write Set

- `04_project_factory/`、`05_harness/`、`06_deployment/` 中与 Pack Classification、Release Audit 和 Recovery 直接相关的文件。
- `SYSTEM.toml`、`AGENTS.md`、`README.md`、`DECISIONS.md`、`CHANGELOG.md`。
- `00_system/schemas/`、`02_registry/`、`07_working/reviews/` 中本次发布的配置、账本与证据。
- `.gitignore` 与本地忽略的 Recovery Artifact 路径。

## Dependencies / Authorization

- Founder 明确指令：“修复后直接发布”。
- 版本按 Patch 递增为 `1.1.3`；直接在本地 `main` 实施，不建立分支。
- 授权范围包含本地 Release Approval 与本地 annotated tag；不包含 Push 或外部发布。

## Expected Output

- Approved Manifest 无漂移。
- V1.1.3 Release Candidate、Recovery Evidence、Release Decision 与本地 annotated tag。
- Tagged HEAD 上 `M1–M6 PASS`。

## Acceptance Criteria

- 两个 Approved Template Manifest 与各自批准基线内容一致。
- Factory 与 M3 仍能正确区分 `PROJECT_SCAFFOLD` 和 `ARTIFACT_LIBRARY`。
- Local Offline CI 8/8 PASS。
- 冷克隆与本地保留 Bundle 恢复副本均通过 CI、Adapter Check、Git fsck 和 Tree Digest 比对。
- M5 验证 Bundle Artifact 实际 SHA-256、Bundle Verify 与 Bundle Head。
- M6 不依赖 Commit 在自身 Tree 中声明自身 Hash。
- `v1.1.3` 为 annotated tag，绑定最终干净 HEAD 并包含 `PAOS-REL-004`。
- `release_audit.py --require-release-ready` 返回 `0`。
- Temp Cleanup 完成安全计划与应用，不执行破坏性删除。

## Validation

- 实现冻结 Commit：`67486a16630c466a6f46710116eadfbcd0c5fff5`。
- Release Candidate Commit：`24de865651a7064c536bb7d223791c38db9af237`。
- Cold Clone / Bundle Clone：Local Offline CI 8/8 PASS、`ADAPTERS_OK`、`git fsck --full` PASS。
- Source / Cold Clone / Bundle Clone Tree SHA-256：`b65993d552bc976331d9fa984842025121662ec4bcf9beaadfe6480b926082d8`。
- Bundle Artifact SHA-256：`183952c02367ca13347a3d920c03ebfb65c1464eb6119a7135fb2eaf218560fb`。
- Clean HEAD Local Offline CI：8/8 PASS；M1–M5 PASS，M6 等待在本文件所在的最终 Release Commit 上创建 annotated tag 后复核。
- Temp Cleanup：计划 `99_temp/plans/gc-20260901T053507Z.json` 已应用，`items = 0`，未删除或移动资产。
- 发布边界：本地 tag `v1.1.3` 获授权；Push、Canonical Promotion、外部部署与 Dashboard 均未执行。
