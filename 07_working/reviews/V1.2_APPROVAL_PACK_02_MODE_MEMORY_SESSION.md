# V1.2 Approval Pack 02｜Mode + Memory + Session

> 状态：`APPROVED`
>
> Review Stage：`DONE`
>
> 日期：`2026-09-01`
>
> Parent Task：`paos-19-v1-2-canonical-promotion-audit`
>
> Approval Reference：`PAOS-017`

## 用途

定义 Personal AI OS 的交互 Mode、最小充分上下文加载、Identity 正文边界、Memory 分层与 Promotion、Conversation 命名、条件式 Session Close 和多 Agent 交接同步协议。

## 层级、维护者与读取者

- 层级：Global Control Plane；生成项目可继承其最小协议，但项目事实仍由项目仓库拥有。
- 维护者：Founder / Governance Owner / Knowledge Owner；Task Owner 只能提交 Working Candidate。
- 读取者：Root Router、Project AGENTS、Session Manager Skill、所有 Agent 与 Handoff/Memory 流程。
- Source of Truth：Founder 批准并绑定固定 Commit 后，本 Pack 成为 Mode、Memory、Session 与 Multi-Agent Sync 的 Canonical Rule Source。

## 与其他文件关系

- Pack 01 提供 Approval、Security、Concurrency 和 Lifecycle 上位规则。
- `AGENTS.md` 定义最小上下文加载入口并路由到本 Pack。
- `01_templates/core-template-pack/` 提供 Profile、Memory、Session Close、Handoff 等结构模板，不替代本 Pack 的规则。
- `.agents/skills/paos-session-manager/` 执行 Session 命名和 Close 建议，不自行产生 Canonical Authority。
- `02_registry/tasks.toml` 保存任务事实；Conversation 标题和 Session Close 不能直接改变 Registry。

## 文件清单

- `00_system/identity/README.md`
- `00_system/modes/MODE_SYSTEM.md`
- `00_system/modes/README.md`
- `00_system/modes/registry.toml`
- `00_system/memory/MEMORY_PIPELINE.md`
- `00_system/memory/README.md`
- `00_system/conversations/SESSION_PROTOCOL.md`
- `00_system/sync/MULTI_AGENT_SYNC.md`

## 初审结论

Pack 的核心设计与 V1.1 Minimum、Root Router、Approved Templates 及现行 Task Registry 一致：Mode 只表达行为边界；Conversation 不是事实源；Memory 必须 Verify 后 Promotion；Session Close 条件触发；多 Agent 写入必须有 Owner 与 Write Set。建议完成以下修正后整包批准。

## 批准前必须修正

1. `MODE_SYSTEM.md` 的 Manifest First 列表漏写“当前 Mode”，与 Root Router 和 `modes/registry.toml` 不一致；应统一为 `Global Governance/Security → Current Mode → Project → Task → Relevant Source/Knowledge`。
2. `MEMORY_PIPELINE.md` 应明确 Memory 中的 `DECISION` 只能引用或摘要已批准 Decision Record，不能替代 `DECISIONS.md`；`memory/README.md` 的简化流程应补回 `Review/Archive`，与正式流程一致。
3. `MULTI_AGENT_SYNC.md` 把所有写入任务绝对要求 Branch/Worktree，与 Pack 01 已批准的 Direct Main 小修订例外冲突；应明确普通/复杂写入使用 Branch/Worktree，只有满足 `PAOS-009` 与 `CONCURRENCY_POLICY.md` 的小修订可以直接本地 `main`。
4. 将“Mode 候选”“Working Spec”等开发阶段措辞更新为 Approved Rule 表述；`modes/registry.toml` 固化稳定 Schema 版本和 Approval Reference。

## Promotion 时的机械变更

- 8 个文件状态由 `WORKING` 更新为 `APPROVED`。
- Markdown 规则统一记录 `Canonical Authority: FOUNDER_APPROVED` 与 Pack Approval Reference。
- `modes/registry.toml` 的 `schema_version` 从 `0.1.0-working` 固化为稳定版本，并将 `artifact_state` 更新为 `APPROVED`。
- 不创建 Identity 正文，不把 Conversation 内容批量写入 Memory。

## 不随本 Pack 批准的内容

- 不批准任何个人 Identity、Preference、Memory 正文或项目事实。
- 不授权跨项目读取、自动 Memory Promotion、自动 Decision 或自动 Task 状态变更。
- 不改变平台 Runtime、Adapter、Factory、Harness、Deployment 或 Skill 权限。
- 不构成 Merge、V1.2 Release、Tag、Push 或外部数据授权。

## 风险与回滚

- 风险：自动 Session Close 可能把过程内容误当长期事实；因此继续要求 Select、Verify、Founder Approval 与项目隔离。
- 风险：多 Agent 同步规则过宽可能阻止合法 Patch；修正后仍只允许明确授权、低风险、可回滚的 Direct Main 例外。
- 风险：Mode 被误解为权限或项目状态；批准文本继续明确 Mode 不扩大授权。
- 回滚：以独立 Promotion Commit 实施；失败时 Git revert，不移动 `v1.1.4` Tag。

## Founder 确认项

1. 同意上述 8 个文件组成 Mode + Memory + Session Pack。
2. 同意实施 4 组修正，再将整包升级为 `APPROVED`。
3. 同意 Memory 中的 Decision 只能引用已批准 Decision Record，不能成为平行事实源。
4. 同意保留 PAOS-009 Direct Main 小修订例外，但普通/复杂并发写入继续使用 Branch/Worktree。
5. 同意本 Pack 不包含任何个人 Memory 正文、跨项目读取或自动 Promotion 权限。

## Founder Confirmation

- 结果：`APPROVED`
- 日期：`2026-09-01`
- 批准范围：同意上述 8 个文件、四组明确修正及状态 Promotion。
- 边界：不批准个人 Memory/Identity 正文、跨项目读取、自动 Promotion、Merge、Tag、Push 或外部传输。
- Evidence：用户明确选择“批准 Pack 02 按审计建议修正并 Promotion”。

本 Pack 已完成内容批准；实施 Commit 与 Tree Digest 将记录在后续 Promotion Evidence 中。V1.2 Canonical 默认生效仍等待合并与 V1.2 Release Evidence。

## Promotion Evidence

- Implementation Commit：`76589100991acbbfbd4f04caaf041a72833cd526`
- Repository Tree SHA-256：`6d15fcc9a00d6a02bbc39681190d6b49a96e61649032f20506384e36cc057c38`
- Evidence File：`07_working/reviews/V1.2_PACK_02_PROMOTION_EVIDENCE.md`
- Local Offline CI：8/8 PASS。
