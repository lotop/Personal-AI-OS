# V1.3.0 Release Task Card

> 资产类别：`SOURCE`
>
> 状态：`WORKING`
>
> 日期：`2026-09-03`
>
> Task ID：`paos-27-v1-3-0-release`
>
> Owner：Founder / `paos-27-v1-3-0-release`
>
> Approval Reference：`PAOS-REL-009`

## Objective

将自 `v1.2.2` 以来的四批工作收敛为 Personal AI OS V1.3.0 本地正式发布，完成双路径恢复演练、annotated tag 绑定与 `main` 远端同步。

## Scope

- 版本对齐：`SYSTEM.toml` 与 `02_registry/projects.toml` 升至 `1.3.0 / v1.3.0 / PAOS-REL-009`。
- 双路径恢复演练：冷克隆与离线 Bundle。
- Release Evidence 固化与本地 annotated tag `v1.3.0`。
- 按 `PAOS-013` 既有授权推送 `main`。

## 本次发布收敛的工作

| 提交 | 内容 | Decision |
|---|---|---|
| `ff93cc5` | 项目创建交互向导 | — |
| `4fc8be2` | 移除 `dashboard/` | `PAOS-025` |
| `c1ae0a8` | Project Base Pack `1.2.0`：四类类型框架、编号目录、项目自带 Harness、`[stack]` | `PAOS-TMPL-004` |
| `4132cf5` | 向导与 Skill 验收改为委托项目校验器 | — |
| `7bc6097` | Project Base Pack `1.3.0`：移除 overlay、交接日志追加式、项目发布约定 | `PAOS-TMPL-005` |

## Out of Scope

- 不推送 annotated tag；`release = "APPROVED_LOCAL_TAG_NOT_PUSHED"` 保持不变。
- 不迁移既有项目；`1.2.x` 及更早生成的项目不与新工厂兼容，是否重建由项目 Owner 决定。
- 不执行破坏性 GC。

## 为什么是次版本而非补丁

模板包发生两次不向后兼容的结构变更：

- 项目目录由 `sources/knowledge/working/archive/tmp` 改为七个编号目录
- 项目类型枚举由五类收敛为四类，旧取值全部失效
- overlay 概念移除，`--overlay` 参数不再存在
- `.paos-init.json` `schema_version` 由 `0.2.0` 升至 `0.3.0`

## Acceptance Criteria

- `validate_repository.py`：`ERRORS=0 WARNINGS=0`。
- `ci_gate.py`：无 `FAIL`。
- 全部单元与集成测试通过。
- 冷克隆与离线 Bundle 双路径演练均 CI 通过、`ADAPTERS_OK`、`git fsck --full` 退出 0。
- Tree Digest 与 Evidence 一致；Bundle 文件名内嵌 Tested Commit 前缀。
- M1~M6 全部 PASS，tag 精确绑定 HEAD。

## Validation

- 实现冻结 Commit：`7bc6097cfe046ffd21180cec73e916a8bb4518f2`。
- 四种项目类型各完成一次真实 `--apply --git` 创建并通过随附校验器。
- 向导端到端跑通（`SOLUTION_RESEARCH`），首次提交纳入 22 个文件，`99_temp/` 被正确忽略。
- 详细恢复证据见 `recovery_evidence.toml` 与 `RECOVERY_DRILL.md`。

## 遗留

- `02_registry/runtimes.toml` 中 `antigravity-cli` 的 `version = "CURRENT"` 仍非可复核版本证据。
- `private_remote_recovery` 保持 `NOT_TESTED`。
- annotated tag 未推送远端。
