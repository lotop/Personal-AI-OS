# V1.2.2 Release Task Card

> 资产类别：`SOURCE`
>
> 状态：`WORKING`
>
> 日期：`2026-09-02`
>
> Task ID：`paos-23-v1-2-2-release`
>
> Owner：Founder / `paos-23-v1-2-2-release`
>
> Approval Reference：`PAOS-024`、`PAOS-REL-008`

## Objective

清理过期过程文件，完成 Personal AI OS V1.2.2 正式发布（双路径恢复演练 + 本地 annotated tag），并按 Founder 授权把 `main` 推送到 origin。

## Scope

- `99_temp/` 过期 GC Plan 与 Quarantine 的一次性清理。
- 版本号与已批准基线由 `1.2.1` 晋升至 `1.2.2`。
- 冷克隆与离线 Bundle 双路径恢复演练，生成 V1.2.2 恢复证据。
- 本地 annotated tag `v1.2.2`。
- `git push origin main`。

## Out of Scope

- 不 push 任何 tag（Founder 确认后明确限定）。
- 不移动、重写或删除既有 tag；`v1.2.1` 保持指向 `15cfee2`。
- 不删除 `99_temp/` 中的部署记录与部署备份。
- 不改变 `gc.toml` 的默认安全策略。
- 不执行外部部署、不启用 Hooks、不授权外部数据。

## Read Set

全仓库（发布审计需要）。

## Write Set

- `SYSTEM.toml`、`02_registry/projects.toml`、`02_registry/tasks.toml`
- `AGENTS.md`、`PROJECT.md`、`README.md`、`DECISIONS.md`、`CHANGELOG.md`
- `07_working/reviews/recovery_evidence.toml`、`RECOVERY_DRILL.md`、本文件
- `06_deployment/recovery_artifacts/v1.2.2-<commit8>.bundle`（新建，不覆盖既有物证）

## Dependencies

- `PAOS-023` / `paos-22-v1-2-1-post-release-remediation`
- `PAOS-024`（Temp 清理授权）
- `PAOS-REL-008`（Release 批准）

## 直接修改 main 与 Push 的授权记录

Founder 于本轮明确指示"清理没有用的过程文件，发 1.2.2，然后 push"。Push 范围经二次确认限定为**只推送 `main`**；annotated tag 保持本地。该授权不包含 tag push、外部部署或远端 Release 发布。

## 清理记录（PAOS-024）

- 删除：`99_temp/plans/` 下 19 个超过 `plan_ttl_hours = 24` 的 GC Plan。
- 删除：`99_temp/quarantine/` 全部批次，内容为 9 个 `.DS_Store`、12 个 `.pyc`、1 个可由 Adapter Generator 重新生成的 `CLAUDE.md` 备份。
- 删除：仓库内 `__pycache__` 目录与 `.DS_Store` 文件。
- 保留：`99_temp/deploy_records/`（4 项）、`99_temp/deployment_records/pack05/`（3 项）、`99_temp/deployment_backups/pack05/`（3 项）——部署证据与回滚材料。
- 结果：`99_temp/` 由约 404KB 降至 44KB。全部删除目标均在 `.gitignore` 覆盖范围内，未进入任何 Commit、Tree Digest 或 Release Bundle，Git 工作树不受影响。

## Expected Output

- 版本、基线、Registry 与文档头部一致指向 `v1.2.2 / PAOS-REL-008`。
- V1.2.2 恢复证据与 Bundle 物证齐备，且不覆盖 V1.2.1 物证。
- M1~M6 全部 `PASS`。
- 本地 annotated tag `v1.2.2` 绑定 Release Commit。
- `origin/main` 与本地 `main` 一致；远端 tag 不变。

## Acceptance Criteria

- `validate_repository.py`：`ERRORS=0 WARNINGS=0`。
- `ci_gate.py --profile local-offline`：无 `FAIL`，`release-state` 显示 `overall=PASS`。
- 冷克隆与 Bundle 克隆恢复到同一 Commit，Tree Digest V0.2 一致，两侧 CI 与 `git fsck --full` 通过。
- 全部单元与集成测试通过。
- `git ls-remote --tags origin` 结果与发布前一致（未推送任何 tag）。

## Validation

- Temp 清理：已执行并复核，见上节。
- Implementation Freeze Commit：`PENDING`。
- Tested Release Commit：`PENDING`。
- Bundle Path：`PENDING`。
- Bundle SHA-256：`PENDING`。
- Tree Digest V0.2 SHA-256：`PENDING`。
- 冷克隆 / Bundle 克隆双路径：`PENDING`。
- 单元与集成测试：`PENDING`。
- M1~M6：`PENDING`。
- Push：`PENDING`。

> 以上 `PENDING` 由实际演练与门禁执行后逐项填入真实值；在演练完成前不得预填任何 Commit、SHA 或结论。

## 遗留未处理项

- `02_registry/runtimes.toml` 中 `antigravity-cli` 的 `version = "CURRENT"` 仍非可复核版本证据。
- 远端 tag 序列在 `v1.1.2` 之后仍为空；`v1.1.3`~`v1.2.2` 只存在于本地，异地恢复依赖本地 Bundle 与本机 Git 历史。
- `private_remote_recovery` 保持 `NOT_TESTED`。
