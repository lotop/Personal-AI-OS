{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "paos://schemas/task-registry/0.2",
  "type": "object",
  "required": ["schema_version", "artifact_state", "allowed_statuses", "allowed_progress", "tasks"],
  "properties": {
    "schema_version": {"type": "string"},
    "artifact_state": {"type": "string", "enum": ["WORKING", "CANDIDATE", "APPROVED", "CANONICAL"]},
    "allowed_statuses": {"type": "array", "items": {"type": "string"}, "minItems": 1, "uniqueItems": true},
    "allowed_progress": {"type": "array", "items": {"type": "string"}, "minItems": 1, "uniqueItems": true},
    "tasks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "id",
          "purpose",
          "status",
          "research_status",
          "local_artifact_status",
          "review_status",
          "implementation_status",
          "platform",
          "platform_ref"
        ],
        "properties": {
          "id": {"type": "string", "pattern": "^paos-[0-9]{2}-[a-z0-9-]+$"},
          "purpose": {"type": "string", "minLength": 1},
          "status": {"type": "string"},
          "research_status": {"type": "string"},
          "local_artifact_status": {"type": "string"},
          "review_status": {"type": "string"},
          "implementation_status": {"type": "string"},
          "platform": {"type": "string"},
          "platform_ref": {"type": "string", "minLength": 1}
        }
      }
    }
  }
}
