# Mode System

> 状态：`APPROVED`
>
> Canonical Authority：`FOUNDER_APPROVED`（`PAOS-017`）

Mode 只用于约束当前交互行为，不改变用户授权范围，也不能覆盖项目事实。

V1.1 Minimum：`CHAT`、`WORK`、`REVIEW`。研究、策略、产品、编码和头脑风暴作为任务标签或 Skill，不形成独立状态机。

完整 Approved Spec 见 `MODE_SYSTEM.md`。
