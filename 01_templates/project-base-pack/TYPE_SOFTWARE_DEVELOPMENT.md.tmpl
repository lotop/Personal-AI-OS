---
name: software-development-framework
description: >-
  {{PROJECT_NAME}} 的软件开发类工作框架。定义技术栈来源、工作循环、
  完成标准与质量红线。当任务涉及写代码、改代码、调试、测试、重构、
  依赖变更或发布时，先读本文件再动手。
---

# 软件开发类约定框架

> 项目：`{{PROJECT_NAME}}`（`{{PROJECT_ID}}`）
>
> 项目类型：`{{PRIMARY_TYPE}}`
>
> 状态：`WORKING`（由 `{{OWNER}}` 按本项目实际情况补全后方可视为项目约定）

本文件是项目类型专属约定，**从属于根目录 `AGENTS.md`**。冲突时以 `AGENTS.md` 与 Owner 当前指令为准。

---

## 1. 技术栈的唯一来源

技术栈的机器可读事实在 `project.toml` 的 `[stack]` 表，**不要在本文件或对话中另立一份**。

```toml
[stack]
language = ""          # 如 python / typescript / go
language_version = ""  # 如 3.11
package_manager = ""   # 如 uv / pnpm / cargo
test_command = ""      # 如 pytest -q
lint_command = ""      # 如 ruff check .
build_command = ""     # 无构建步骤则留空
run_command = ""       # 本地启动命令
```

依赖清单使用**语言原生文件**（`pyproject.toml`、`package.json`、`go.mod` 等），不发明新格式。

**Agent 行为约定：**

- 需要跑测试/构建/lint 时，读 `project.toml` 的 `[stack]` 取命令，不要猜测也不要自行安装工具链。
- 字段为空时，先问 Owner，不要擅自选型。选型属于技术决策，须记入 `DECISIONS.md`。
- 变更技术栈、引入新依赖、升级主版本，都要先在 `DECISIONS.md` 立决策记录再改代码。

---

## 2. 工作循环

每个非简单任务走完四步，任何一步的产出都要能被下一步检验。

| 阶段 | 输入 | 动作 | 产出 |
|---|---|---|---|
| **理解** | Task Card、相关代码 | 定位改动点，读懂现状 | Read Set 与 Write Set 收敛为具体文件 |
| **实施** | 确认后的 Write Set | 写代码，风格随周边代码 | 可编译/可运行的改动 |
| **验证** | `[stack].test_command` 等 | 跑测试、lint、必要时手动验证 | 真实命令输出 |
| **交付** | 验证证据 | 说明改了什么、验证了什么、还差什么 | 结论 + 证据 |

**验证不是可选项。** 未跑通 `test_command` 不得声称完成；跑不通就如实报告失败输出。

---

## 3. 完成的定义

一项开发任务只有同时满足以下条件才算完成：

- [ ] 代码改动落盘，且能被 `[stack]` 中声明的命令构建/运行
- [ ] `test_command` 通过；新增行为有对应测试
- [ ] `lint_command` 通过（若已声明）
- [ ] 不破坏既有公开接口，或破坏已在 `DECISIONS.md` 记录
- [ ] 改动范围不超出 Task Card 的 Write Set
- [ ] 交付说明分别列出：已修改、已验证、未完成、需决策

**"我觉得应该没问题"不是验证。** 没有命令输出就是没有验证。

---

## 4. 代码与仓库约定

- **风格随周边代码**：命名、注释密度、错误处理方式向同文件和同目录看齐，不引入个人偏好。
- **最小改动**：修 bug 就修 bug，不顺手重构；重构单独立任务。
- **不擅自新增**：不主动加文档、changelog、格式化、覆盖率配置，除非任务要求。
- **提交信息**说明「做了什么 + 为什么」，不复述 diff。
- **分支策略**由 Owner 在下方补全；未约定前不直接向默认分支推送。

```
分支约定（待 {{OWNER}} 补全）：
主分支：
特性分支命名：
合并方式：
```

---

## 5. 质量红线

以下动作**必须**先取得 Owner 明确授权：

- 删除或重写既有文件的大段逻辑
- 引入新的运行时依赖或更换核心库
- 修改构建、CI、部署配置
- 任何 `git push`、发布、对外部署
- 触碰密钥、令牌、生产数据或真实用户数据

以下动作**永远禁止**：

- 把真实 Secret 写进代码、配置、日志或提交
- 为了让测试通过而修改测试断言或跳过测试
- 在无法验证的情况下声称"已修复"或"已部署"

---

## 6. 目录落位

| 内容 | 位置 |
|---|---|
| 源代码 | 由 Owner 在 `[stack]` 与本节约定后确定（如 `src/`） |
| 外部规格、需求原件、第三方文档 | `01_sources/`（只读） |
| 已验证的技术结论、架构说明 | `02_knowledge/` |
| 项目自己的校验与工具脚本 | `05_harness/` |
| 草稿、实验、临时分析 | `07_working/` |
| 被替代但需追溯的旧方案 | `09_archive/` |
| 构建中间产物、缓存 | `99_temp/`（可清理，不提交） |

---

## 7. 待 Owner 补全

以下内容工厂不预填，须由 `{{OWNER}}` 按项目实际情况填写后，本框架才算生效：

- `project.toml` 的 `[stack]` 全部字段
- 第 4 节的分支约定
- 第 6 节的源代码目录
- 本项目特有的架构约束或性能要求（如有）
