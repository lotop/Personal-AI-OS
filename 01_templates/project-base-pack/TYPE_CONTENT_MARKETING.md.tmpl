---
name: content-marketing-framework
description: >-
  {{PROJECT_NAME}} 的内容营销类工作框架。定义受众与语气、内容生产流程、
  事实核查与发布授权边界。当任务涉及写文案、做内容策划、渠道分发
  或数据复盘时，先读本文件再动手。
---

# 内容营销类约定框架

> 项目：`{{PROJECT_NAME}}`（`{{PROJECT_ID}}`）
>
> 项目类型：`{{PRIMARY_TYPE}}`
>
> 状态：`WORKING`（由 `{{OWNER}}` 按本项目实际情况补全后方可视为项目约定）

本文件是项目类型专属约定，**从属于根目录 `AGENTS.md`**。冲突时以 `AGENTS.md` 与 Owner 当前指令为准。

---

## 1. 内容的三个前置问题

动笔前必须先答清楚，答不出就先问 Owner，不要凭想象补：

1. **写给谁**（受众画像、他们已经知道什么、在意什么）
2. **要他做什么**（唯一的行动目标，不是"提升认知"这种无法验证的说法）
3. **发在哪**（渠道决定长度、语气、格式与合规要求）

三者任一为空时产出的内容一律视为草稿，不进入发布流程。

---

## 2. 受众与语气

```
目标受众（待 {{OWNER}} 补全）：
他们当前的认知起点：
本项目统一语气：
禁用表达：
必须使用的术语／叫法：
不得使用的术语／叫法：
```

**语气一致性优先于单篇出彩。** 一篇风格突出但偏离整体语气的内容，对项目是负资产。

---

## 3. 内容生产流程

| 阶段 | 动作 | 产出 | 落位 |
|---|---|---|---|
| **选题** | 明确受众、行动目标、渠道 | 选题卡 | `TASKS.md` |
| **取材** | 收集产品事实、数据、案例原件 | 原始材料 | `01_sources/`（只读） |
| **起草** | 按渠道格式写作 | 草稿 | `07_working/` |
| **核查** | 逐条核对事实与数据 | 核查记录 | 与草稿同目录 |
| **定稿** | Owner 审定 | 终稿 | `02_knowledge/` |
| **发布** | 经明确授权后分发 | 发布记录 | `07_working/` |
| **复盘** | 记录实际表现 | 数据与结论 | `02_knowledge/` |

---

## 4. 事实核查（不可跳过）

每一条以下内容都要能指回 `01_sources/` 里的原件：

- 产品能力、参数、价格、可用性
- 数字、比例、排名、增长率
- 客户名称、案例细节、引述
- 对比竞品的任何表述
- 时间、期限、活动规则

**红线：**

- **不得编造**数据、案例、客户名、用户评价、引述。
- **不得夸大**产品尚不具备的能力；没有的功能不写成"支持"。
- 无法核实的表述要么删掉，要么改成有明确限定的说法，**不要模糊化后保留**。
- 竞品对比必须基于公开可核验的信息，并注明获取日期。

核查未完成的稿件**不得进入发布流程**，即使 deadline 到了。

---

## 5. 发布授权

**以下动作一律需要 Owner 逐次明确授权，不存在"默认可发"：**

- 在任何公开渠道发布、投放、推送内容
- 修改已发布内容
- 以项目或公司名义回复公开评论
- 向外部名单发送邮件或消息
- 提交内容到第三方平台或代理机构

Agent 可以准备好待发布内容并说明"已就绪，等待授权"，**但不得自行触发任何对外动作**。

---

## 6. 效果复盘

发布后记录实际数据，不做美化：

```markdown
## 内容
（标题、渠道、发布日期、链接）

## 目标
（当初设定的唯一行动目标）

## 实际数据
（原始数字，注明统计口径与取数日期）

## 与预期的差距
（如实写，包括低于预期的情况）

## 下次改什么
（具体到可执行的一条）
```

**表现不佳的内容同样要记录。** 只记成功案例会让 `02_knowledge/` 失去参考价值。

---

## 7. 目录落位

| 内容 | 位置 |
|---|---|
| 产品事实、数据原件、素材原图 | `01_sources/`（只读） |
| 定稿内容、复盘结论、可复用模板 | `02_knowledge/` |
| 核查脚本、术语一致性检查 | `05_harness/` |
| 草稿、多版本比稿、核查记录 | `07_working/` |
| 已下线或被替代的内容 | `09_archive/` |
| 导出中间件、临时素材 | `99_temp/`（可清理，不提交） |

---

## 8. 待 Owner 补全

- 第 2 节的受众、语气、术语表
- 各渠道的格式与字数约定
- 需要法务或合规审阅的内容类型
- 数据统计口径与取数来源
