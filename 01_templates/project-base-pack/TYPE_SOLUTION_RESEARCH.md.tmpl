---
name: solution-research-framework
description: >-
  {{PROJECT_NAME}} 的方案调研类工作框架。定义来源分级、证据要求、
  结论表达与不确定性处理。当任务涉及调研、比选、可行性分析、
  技术选型或形成决策建议时，先读本文件再动手。
---

# 方案调研类约定框架

> 项目：`{{PROJECT_NAME}}`（`{{PROJECT_ID}}`）
>
> 项目类型：`{{PRIMARY_TYPE}}`
>
> 状态：`WORKING`（由 `{{OWNER}}` 按本项目实际情况补全后方可视为项目约定）

本文件是项目类型专属约定，**从属于根目录 `AGENTS.md`**。冲突时以 `AGENTS.md` 与 Owner 当前指令为准。

---

## 1. 调研的第一原则

**区分"查到的"和"推出的"。** 本项目所有产出必须能回答三个问题：

1. 这条结论的依据是什么？
2. 依据来自哪里，什么时候获取的？
3. 如果依据是错的，结论会怎样改变？

回答不了第三个问题的结论，标记为 `ASSUMPTION` 而不是 `FINDING`。

---

## 2. 来源分级

每条关键事实都要带来源等级。写进 `02_knowledge/` 的内容必须注明等级与获取日期。

| 等级 | 含义 | 例子 |
|---|---|---|
| **A｜一手权威** | 官方文档、规范原文、源码、法规条文、厂商正式声明 | 官方 API 文档、RFC、财报原文 |
| **B｜可核验二手** | 有署名、有数据、可追溯到 A 级来源的分析 | 技术白皮书、有引用的行业报告 |
| **C｜参考性** | 无法追溯到原始依据的经验、博客、论坛、口碑 | 个人博客、社区讨论 |
| **D｜模型推断** | 由 Agent 或人基于已有信息推理得出，无外部来源 | "按此推算约为…" |

**红线：**

- **D 级永远不能伪装成 A/B 级。** 不确定就写"未核实"，不要补一个看起来像来源的东西。
- 关键决策依据**至少要有一条 A 级或两条独立 B 级**支撑。
- 只有 C/D 级支撑的结论，必须在结论旁明写"证据不足"。

---

## 3. 工作循环

| 阶段 | 动作 | 产出 | 落位 |
|---|---|---|---|
| **界定** | 明确要回答的问题、判断标准、排除范围 | 调研问题清单 + 评估维度 | `TASKS.md` |
| **收集** | 按来源分级取证，原件不改写 | 原始材料 | `01_sources/`（只读） |
| **分析** | 对齐维度做横向比较，标注不确定性 | 分析草稿 | `07_working/` |
| **结论** | 形成建议 + 置信度 + 反驳条件 | 结论文档 | `02_knowledge/` |
| **决策** | Owner 拍板后记录 | 决策记录 | `DECISIONS.md` |

**原件进 `01_sources/` 后不得改写。** 需要摘录、翻译、整理，产出新文件放 `07_working/`，并注明原件路径。

---

## 4. 结论的表达方式

每份结论文档至少包含：

```markdown
## 结论
（一句话的建议）

## 置信度
高 / 中 / 低 —— 并说明为什么是这个等级

## 关键依据
- [A] 依据一（来源，获取日期）
- [B] 依据二（来源，获取日期）

## 会推翻本结论的条件
- 如果…成立，则应改为…

## 未解决的问题
- （明确列出没查清的部分，不要略过）
```

**"未解决的问题"一节不能为空。** 任何调研都有边界，写不出边界说明调研深度不够。

---

## 5. 方案比选的格式

多方案比较必须先定评估维度再填表，**不允许边比边改维度**（那会变成为预设结论找理由）。

| 维度 | 权重 | 方案 A | 方案 B | 方案 C |
|---|---|---|---|---|
| （由 Owner 定义） | | | | |

要求：

- 维度和权重在收集证据**之前**确定，写进 `TASKS.md`。
- 每个格子填事实与来源等级，不填主观评分词。
- 最终推荐必须能由表格推出；推不出就说明还有表外因素，并把该因素补进维度。

---

## 6. 质量红线

**永远禁止：**

- 编造来源、链接、数据、引用或日期
- 把模型推断（D 级）写成查证结果
- 用"业界普遍认为""通常来说"掩盖没有来源的断言
- 为了让结论更干净而隐去相互矛盾的证据

**必须授权：**

- 访问外部网络、调用付费数据源、传输项目资料到外部服务
- 联系外部人员获取信息
- 把调研结论对外发布

---

## 7. 目录落位

| 内容 | 位置 |
|---|---|
| 原始材料、原件、下载的报告 | `01_sources/`（只读） |
| 已确认的结论与长期知识 | `02_knowledge/` |
| 校验脚本、引用检查工具 | `05_harness/` |
| 分析草稿、中间比较表 | `07_working/` |
| 被新结论替代的旧版本 | `09_archive/` |
| 临时下载、缓存 | `99_temp/`（可清理，不提交） |

---

## 8. 待 Owner 补全

- 本项目的评估维度与权重（第 5 节）
- 置信度等级的判定标准（什么算"高"）
- 允许访问的外部来源范围
- 结论文档的命名与版本约定
