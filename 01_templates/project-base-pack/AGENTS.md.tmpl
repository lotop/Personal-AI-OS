# {{PROJECT_NAME}}

> Project ID：`{{PROJECT_ID}}`
>
> Owner：`{{OWNER}}`
>
> 项目类型：`{{PRIMARY_TYPE}}`
>
> Template Version：`{{TEMPLATE_VERSION}}`
>
> 实例状态：`PROVISIONAL`（经项目 Owner 验收后方可调整）

## 项目入口

本仓库是 `{{PROJECT_NAME}}` 的独立项目仓库。项目目标、范围和成功标准以 `PROJECT.md` 为准，机器可读元数据以 `project.toml` 为准。

处理非简单任务前，按最小充分原则读取：

1. 本文件及项目级已批准规则。
2. `00_governance/PROJECT_TYPE_FRAMEWORK.md`（本项目类型的专属工作约定）。
3. `PROJECT.md`、`project.toml` 和适用 Decisions。
4. `TASKS.md` 中的当前 Task Card。
5. 与任务直接相关的 Source、Knowledge 和文件。

不得默认加载全部历史、归档、其他项目或无关资料。

## 项目类型约定

本项目类型为 `{{PRIMARY_TYPE}}`，其专属工作约定位于 `00_governance/PROJECT_TYPE_FRAMEWORK.md`，内容包括该类型的工作循环、完成标准、质量红线与目录落位。

**涉及该类型核心工作的任务，动手前必须先读该文件。** 它从属于本文件；两者冲突时以本文件与 Owner 当前指令为准。

类型框架初始状态为 `WORKING`，其中标注"待 Owner 补全"的部分在填写前不构成约束。

## 目录结构与 Source of Truth

| 目录 | 用途 | 约束 |
|---|---|---|
| `00_governance/` | 项目级规则与类型约定框架 | 变更须经 Owner 批准并记入 `DECISIONS.md` |
| `01_sources/` | 原始输入 | **只读**，不静默改写；需要加工时产出新文件 |
| `02_knowledge/` | 项目批准后的长期知识 | 须可追溯到来源或决策 |
| `05_harness/` | 项目自己的校验与迭代工具 | 只读校验，不自动修复 |
| `07_working/` | Working 产物 | 不具有 Canonical Authority |
| `09_archive/` | 已替代但需追溯的材料 | 归档不删除 |
| `99_temp/` | 临时运行资产 | 可按批准的 GC 规则清理，不提交 |

Conversation、AI 总结和 Generated 文件不得自动成为项目事实。

## Task Card

复杂任务必须在 `TASKS.md` 声明 Objective、Scope、Non-goals、Read Set、Write Set、Dependencies、Expected Output、Acceptance Criteria、Owner 和 Permissions。并行任务的 Write Set 不得重叠。

## 校验与状态推进

```bash
python3 05_harness/validate_project.py                    # 日常自检
python3 05_harness/validate_project.py --require-active    # 转 ACTIVE 前的门槛
```

项目初始为 `PROVISIONAL`。转 `ACTIVE` 前 `--require-active` 必须退出码为 `0`，且 `project.toml` 与 `PROJECT.md` 两处状态同步更新。详见 `05_harness/HARNESS.md`。

## 自我迭代

本项目的约定可以演进。发现某条约定不适用时，**修改它而不是绕过它**：在 `07_working/` 提案 → Owner 批准并记入 `DECISIONS.md` → 修改 `00_governance/` 或本文件 → 旧版本移入 `09_archive/` → 重跑校验器。

校验器本身也可扩展；新增检查时同步更新 `05_harness/HARNESS.md`。

迭代本项目不会改变 Personal AI OS 的模板；若某条改进对所有项目都成立，反馈给 Personal AI OS 决定是否进入下一版 Template Pack。

## 权限与安全

- 使用完成任务所需的最小权限。
- 不读取、输出或提交真实 Secret。
- 不静默覆盖 Source、Approved 或 Canonical 内容。
- 删除、部署、外部发布、外部数据传输、权限升级和破坏性清理需要明确授权。
- Generated Adapter 必须可追溯到批准的 Source 和生成器版本。

## 完成协议

复杂任务至少完成：理解范围、实施、验证和交付。Cleanup 按资产风险执行。

宣布完成时分别说明：已修改、已验证、已提交、已部署、尚未完成和需要决策。未经验证不得声称部署或恢复成功。

## 交接与会话收尾

规则在本节；记录写进对应的追加式日志文件，**不要把规则复制进日志**。

**Handoff（`HANDOFF.md`）** —— 只在发生真实交接时记录。至少包含：发送方、接收方、目标、当前 Commit/Branch、已完成、未完成、验证证据、风险、权限边界、下一步。只有并行写入或存在冲突的任务才需要完整 Read/Write Set。

接收方必须重新验证高风险、时效性或外部状态，**不得仅凭交接文本宣布完成**。

**Session Close（`SESSION_CLOSE.md`）** —— 只在交接、跨环境、长暂停、Blocked 或形成持久结论时记录。至少包含：日期、Objective、Completed、Files/Evidence、Validation、Decisions、Assumptions、Risks、Next Action。

会话总结本身不是项目事实，须经对应事实来源或审批记录验证。

两个日志都是追加式，最新记录在最上方，已有记录不删除、不改写。

## 版本与发布

项目自己的版本与发布节奏由 Owner 定义，Personal AI OS 不代为规定。但一旦发布，必须满足：

- 变更记入 `CHANGELOG.md`；未发布的改动先进 `Unreleased` 段。
- 每个版本绑定一个确定的 Commit SHA；`CHANGELOG.md` 中写全 40 位。
- 发布前 `python3 05_harness/validate_project.py` 退出码为 `0`，工作树干净。
- 发布是不可逆动作，**须经 Owner 明确授权**；Agent 不得自行 tag、push 或对外发布。

```
本项目的发布约定（待 {{OWNER}} 补全）：
版本号规则：
发布触发条件：
需要同步更新的位置：
```

## 冲突处理

当用户当前指令、项目规则、类型框架、Task Card 和 Agent 默认行为冲突时，不静默选择。影响目标、权限、Source of Truth 或正式交付物的冲突必须交给 Project Owner 决定。
